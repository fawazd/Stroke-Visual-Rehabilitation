using UnityEngine;
using UnityEngine.UI;

public class BS_Score : MonoBehaviour {

    public Text scoreText;
    int score = 0;
    int increment = 0;
    public int scoreSpeed = 10;

	// Update is called once per frame
	void Update () {
        //increment++;
        //if (increment % scoreSpeed == 0)
        //{
        //   score++;
        //    scoreText.text = score.ToString();
        //}
	}
}
